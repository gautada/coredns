#!/bin/sh
# s6 service: launches CoreDNS with the mounted Corefile.
# Corefile and zone files are symlinked into /etc/container via the Containerfile.
exec /usr/bin/coredns -conf /etc/container/Corefile
